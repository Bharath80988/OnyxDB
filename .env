# OnyxDB Environment Variables

# Security / RBAC Tokens
# Passed in HTTP Headers as: Authorization: Bearer <key>
ADMIN_TOKEN=Bearer admin-secret-key
READ_ONLY_TOKEN=Bearer readonly-secret-key

# Storage Path (Used by Docker and local fallback)
DB_STORAGE_PATH=~/OnyxDB/database

# API Server Port
SERVER_PORT=8080
